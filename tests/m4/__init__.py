"""M4 (integration-demo) test suite.

The M4 gate (B4) requires:

  * INT-DIFY-001 — Dify Task Tool Adapter (3 modes + governance state)
  * INT-AH-001   — AgenticHub MCP/API Adapter (3 modes + governance state)
  * DEMO-001     — same Reference Scenario, same security semantics
  * OSS-001      — CLI, Compose/Helm, clean-room install
  * 3 delegation modes have contract tests proving ownership
"""
